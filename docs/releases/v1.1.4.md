# iPaper 1.1.4 — 品牌与部署迁移

PaperPilot 的当前产品标识统一为 **iPaper**；Python 包、管理员命令、服务和三个镜像仓库使用 `ipaper`，正式配置使用 `IPAPER_`。本版本仅迁移品牌及基础设施，不新增 Agent、翻译或其他产品功能，不改变数据库 schema、资产 ID、聊天协议或许可证。

源码：`3855aee0e090f5f2c28839a637393a1c3f6cd0b0`（不可变 `v1.1.4`）；平台 `linux/amd64`。后续发布记录补充提交不改变本次构建源码。

| 组件 | 已回拉核验的完整引用 |
| --- | --- |
| ipaper | `ifzzh520/ipaper:1.1.4@sha256:64984853e6dcfc4431dbc53694fdbce564928b9941a7599073c2d1b3dcabc685` |
| ipaper-document-worker | `ifzzh520/ipaper-document-worker:1.1.4@sha256:49fca76add0ae3d65ad64552564597792ab461cda5542c3c35294eb96b1e5f69` |
| ipaper-translation-worker | `ifzzh520/ipaper-translation-worker:1.1.4@sha256:16f1352be1674840ee2a4b2b06adcfb791ec85703eb8e32991f478763ab148f0` |

三个组件的包和启动入口均发生变化，因此本次均构建 1.1.4。旧历史镜像独立复制，未重新构建：Web 25、Translation 15、Document 12，共 **52 个版本标签**；27 项旧 Worker 映射逐项对账一致。逐标签 digest、清单类型、平台和状态见附件 `image-history.json`，所有目标清单及平台层已重新读取/检查。源仓库现已不存在的集中 Worker 后缀标签按历史清单说明，不补造标签。旧仓库和标签全部保留；Worker 不新增 latest。

## 本轮验证

- 确定提交归档构建；完整 Docker 非集成测试 **263 passed、1 deselected、84 subtests**。干净前端安装、类型检查、9 项单元测试、13 项 Edge 测试、构建和 npm audit（0）通过；旧前端安全回归通过。
- 最终三个镜像的隔离启动、真实 Flask 登录、冷启动列表、PDF Range、自托管产物与非 root 检查通过；没有挂载生产数据。新旧环境变量优先级、持久加密 AAD/分类 namespace、账号与 owner 隔离回归通过。
- 四份 Python 依赖报告、源码/secret 扫描、三个最终组件镜像扫描及 SBOM 通过现有安全门禁。各运行镜像仍有 **75 Medium、9 Low** 记录，不宣称零漏洞。既有 ignore-unfixed 和逐项例外策略保持；八项有到期日的例外（setuptools、jaraco.context、wheel、Starlette）完整附在 `vulnerability-exceptions.json`，到期日 2026-12-08，不扩大适用范围。
- GitHub CI：main [34679961318](https://github.com/ifzzh/iPaper/actions/runs/34679961318)、dev [34679961439](https://github.com/ifzzh/iPaper/actions/runs/34679961439)、personal [34679961481](https://github.com/ifzzh/iPaper/actions/runs/34679961481) 全部成功。
- 正式迁移：等待既有任务空闲后停写；同文件系统改名、SQLite checkpoint/一致性备份、原 external Document 卷连续挂载。三个新服务健康，`/readyz`、匿名 Worker 拒绝与内部认证探针通过；原三实例停止，资源限制、端口和隔离不变。
- 生产 **607 个资产**校验一致，schema 未改、账号/论文/历史记录保留、密钥字节不变，四项既有加密配置可解密。新路径备份脚本已实际执行成功。
- 正式 Edge：AutoSci 原文及已有译文均 **22 页**，实际文本与非空画面、12 张阅读视图及 6 张文献库视图（1920/1440/390、浅/深）通过；三篇论文、历史、任务日志、Daily、设置、退出及跨标签清理通过。优雅重启后首次深链接仍恢复第 5 页、75% 缩放及稳定页内锚点。生产截图仅私有保存。

生产检查使用服务端创建并清理的短时 ifzzh 验收会话，没有读取或重置用户密码；密码登录及变更性操作使用隔离合成库，不伪称生产密码输入验收。计时写入被拦截以避免人工累计，聊天 POST 被阻止；本轮没有新增付费模型或 MinerU 验收调用。既有 Daily 调度任务正常运行，不混记为验收任务。性能测量沿用历史资料，本次未重新跑性能基准。

## 迁移与兼容

路径映射、原持久卷绑定与操作原则见 [迁移指南](https://github.com/ifzzh/iPaper/blob/main/docs/operations/ipaper-migration.md)。旧路径临时保留符号链接；`paperpilot_session`/`paperpilot_csrf`、分类 UUID namespace、加密 AAD 和迁移标记保留兼容。旧环境变量仍可读，显式新变量优先。上游 PaperPilot、PaperQuay、Resophy 的署名及历史版本内容保留。

回退使用本次受限备份内的专用脚本：确认空闲、停新服务、恢复旧路径/配置与 Web 1.1.3 + Translation 0.10.5 + Document 1.0.0，继续使用当前数据库，不能以旧备份覆盖新增数据。脚本同时恢复新 Web 渠道至已复制的旧稳定摘要；旧仓库 latest 保持迁移前稳定版本。正式迁移成功后未为演练再次回退；隔离 SQLite 路径往返及新增行保留测试通过。

仓库：[GitHub](https://github.com/ifzzh/iPaper) · [Web](https://hub.docker.com/r/ifzzh520/ipaper) · [Translation](https://hub.docker.com/r/ifzzh520/ipaper-translation-worker) · [Document](https://hub.docker.com/r/ifzzh520/ipaper-document-worker)。

Web 渠道远端核验：新 `ipaper:latest` 为 `sha256:64984853e6dcfc4431dbc53694fdbce564928b9941a7599073c2d1b3dcabc685`；旧 `paperpilot:latest` 保持 `sha256:c0746062decd27b5701c34674dd7ef9e77ed7ff69ad14e8fd5be10026ee9ca0c`。两个 Worker 仓库均无 latest。
