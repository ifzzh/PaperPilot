# iPaper 1.2.0 — 双翻译阅读与来源定位

状态：已发布并部署；最终提交、三组件远端摘要、安全门禁与正式验收记录见下方及 Release 附件。

同一论文可保留 BabelDOC 纯译文/双语 PDF，并使用 MinerU 解析后的结构化原文、译文或双语内容。切换已有结果不创建任务；生成弹窗显示语言、独立模型、页码范围、缓存和预算。单块重译失败保留旧译文，部分成功可继续阅读。

结构内容按阅读顺序显示段落、公式、表格、图片及图注，按需加载。有效来源可以跳回原始 PDF 并返回结构位置。选区问答使用服务端验证的来源，只有实际进入模型上下文且被引用的编号成为链接；旧聊天不补造来源。

## 兼容与限制

- 现有原文、版式译文、账号、聊天、任务与原始/译文位置保留。新结果使用增量 `processing_*` 表和 owner 隔离不可变文件，没有第四个常驻服务。
- 独立结构模型仅在未配置时从 ifzzh 的 BabelDOC 设置由服务端初始化。密钥采用独立表和用途加密；旧镜像仍能读取其原有密钥表。
- Document Worker 先校验并按每段最多 200 页分段。当前输入总限额 2,000 页/100 MiB，结构任务默认 200 请求、50 万输入/25 万输出 token 保守预留及两小时；每请求 120 秒硬截止。超时或未确认请求不自动重发。
- 精确区域只来自已验证的 MinerU schema/页面变换；未知、旋转或裁剪等未验证转换降为页级，没有可靠页码则不跳转。BabelDOC PDF 不推断与原文逐页对应。
- 全局结构并发两个、每用户一个活动任务，等待队列 20。默认每结果 1 GiB、每用户 10 GiB；临时检查点 7 天、详细事件 30 天。旧结果、成功译文和引用不自动删除。
- 独立实现参考固定 PaperQuay 0.1.25 提交，未搬入 AGPL/Electron 代码。保留 PaperPilot/Resophy/PaperQuay 来源及原许可事实；KaTeX 为 MIT 依赖。

## 组件组合

三个组件的执行代码或输出契约都发生变化，均发布 1.2.0；Registry 清单和所有镜像回拉已核验，正式 Compose 固定摘要。

| 组件 | 仓库 | 版本 |
| --- | --- | --- |
| Web | `ifzzh520/ipaper` | `1.2.0` |
| Translation Worker | `ifzzh520/ipaper-translation-worker` | `1.2.0` |
| Document Worker | `ifzzh520/ipaper-document-worker` | `1.2.0` |

## 验证记录

本轮经授权只创建一次 22 页 MinerU 云解析，得到 237 个结构块；使用配置的 qwen3.8-flash 翻译首页 6 块，并对其中一块重译（结构模型实际共 7 次请求）。另一次来源问答输出上限 512 token，回答和可信引用已持久保存，重新登录读取一致。没有追加真实请求，缓存命中与阅读切换不调用模型。仅这 6 块属于本次真实翻译范围，其余内容明确显示未译。

真实原文、已有双语 PDF 均为 22 页；结构标题/摘要区域与原文实际对应，三种内容切换、来源返回、历史问答和刷新恢复通过。1920/1440/390 浅深截图私有保存。真实产物中的上下标采用限定 React 呈现，并核对 canonical UTF-16 选区；任意 HTML/属性仍按文本显示。

离线/重放证据：两页历史 MinerU ZIP 经真实 Worker HTTP 验证 34 块（31 区域/3 页级）；实际 BabelDOC 0.6.4 程序配合假模型验证纯译文/双语输出；错误、取消、跨用户隔离与恢复使用合成数据。最终完整测试、安全报告及三 SBOM 以附件的提交和镜像标识为准，不把先前候选的结果混记为最终镜像。

既有漏洞例外未扩大，适用目标、理由和到期日见 [例外清单](../../security/vulnerability-exceptions.json)；最终扫描记录包含实际应用情况。正式验收成功后才移动 Web latest，Worker 不新增 latest。

## 备份与回退

升级前检查活动任务，优雅停止写入，保存 SQLite 一致性快照、匹配不可变产物、完整论文树、受限配置和原主密钥。正式 Compose 固定三个版本摘要，保留现有 `deploy_document_jobs` external 卷、资产路径与 UID/GID，不使用 `down -v`。

回退恢复本次部署前的三镜像、配置与 Web latest，保留当前数据库及新增产物。旧镜像可继续写旧功能数据；再次升级读取保留的新结果。不得用旧库备份覆盖部署后新增用户数据。持久备份包含独立 `rollback.py`；不执行品牌改名时期的路径回退。操作详见[结构产物备份与回退](../operations/structured-backup.md)。

[实现说明](../development/dual-translation.md) · [PaperQuay 适配](../development/paperquay-adaptation.md) · [部署指南](../operations/deployment.md)

## 最终发布与正式验收

- 发布提交 `8680e9512f0a78065f9bf5c12a06a9d9b5f76004`，平台 `linux/amd64`；三个发布分支 GitHub Security gate 全部成功。
- Docker 非集成 309 项、84 子断言；前端 11 项单测、类型检查、构建与审计；13 项统一界面 Edge 回归及独立结构阅读流程通过。扫描最终三镜像并生成匹配 SBOM，既有漏洞例外没有扩大。
- 正式 AutoSci 原文及已有双语 PDF 均真实显示 22 页；三服务重启、三篇论文切换、独立 PDF/结构位置与会话恢复通过；结构内容、标题/摘要区域、来源问答历史、刷新、Daily、任务日志、设置及退出跨标签清理通过。18 个桌面/移动浅深视图只存私有验收目录。正式浏览器采用 15 分钟维护会话且结束删除；实际密码登录和账号修改流程为隔离 Edge 回归证据，未更改用户密码。
- 发布前 611 个既有资产文件大小和 SHA-256 全部一致，309 篇论文、账号、原密钥和旧聊天/任务历史保留；仅新增已验证的结构结果及一段来源问答，正式部署阶段零供应商请求。
- 首次产物预检因一次性迁移容器权限不足拒绝，自动恢复 1.1.4 并验证健康；修正运维容器权限后重新备份、提升及部署成功。该问题未改变运行源码或已发布 tag。新表及独立配置在实际旧镜像回退期间保留。
- 日常备份现包含 SQLite 及其引用的不可变产物，已实际验证 50 个产物文件。持久回退工具经过 dry-run，仍恢复三个独立 ipaper 仓库的 1.1.4 摘要，保留当前数据库与新结果。

- **Web**：`ifzzh520/ipaper:1.2.0@sha256:914467d6a33bd80dc1bedddd8ecf373d6bf17c1d4d573ecd56b26958dbd1428a`
- **Translation Worker**：`ifzzh520/ipaper-translation-worker:1.2.0@sha256:a5fa3b4316802c15e99a7387baa49604166fb6092a1aca34b8c5e34de3d52003`
- **Document Worker**：`ifzzh520/ipaper-document-worker:1.2.0@sha256:2c501ad68bdfc2e282518a813c46e548f598b1af85acc1069058741a45587298`

Web `latest` 已在正式验收后更新到上述 Web 摘要，并经 Registry 回读与 Docker 拉取再次核验。两个 Worker 仓库仍没有 `latest`。
